// Mirrors backend/src/config/academics.js. Add new entries in both places.
export const PROGRAMS = ['PGPM', 'PGDM', 'MBA'];

export const SECTIONS = ['1', '2', '3'];

export const sectionLabel = (s) => `Section ${s}`;
