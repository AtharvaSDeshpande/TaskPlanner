# Production mode — the deployed build talks directly to the Render backend.
# The API is served under /api, so the base must include it.
VITE_APP_ENV=production
VITE_API_BASE_URL=https://taskplanner-mor2.onrender.com/api
